import React from 'react'

export default function ItemLinkPage({ title, path }) {
    return (
        // <Link to = {path}>
            <span className='text-blue-800'>{title}</span>
        // </Link>
    )
}
