import React from 'react'

export default function ItemLinkPage({ title, path }) {
    return (
        // <Link to = {path}>
            <span className='text-gray-800 mx-1'>{title}</span>
        // </Link>
    )
}
